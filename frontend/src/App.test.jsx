import { describe, it, expect } from 'vitest';

describe('Configuración inicial frontend', () => {
  it('el pipeline de pruebas de React debe ejecutarse sin errores', () => {
    expect(true).toBe(true);
  });
});
